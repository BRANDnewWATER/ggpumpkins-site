# Repo is public; Netlify auto-deploys on every push to main. Publish dir = site root.

[build]
  publish = "."

[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
